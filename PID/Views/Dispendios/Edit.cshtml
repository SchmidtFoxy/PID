@model PID.Models.Dispendio

@{
    ViewData["Title"] = "Editar Dispêndio";
}

<div class="container py-5">
    <div class="row mb-5">
        <div class="col-md-12 text-center">
            <h1 class="display-2 fw-bold">Editar Dispêndio</h1>
            <p class="lead mt-3">Atualize as informações abaixo do dispêndio selecionado.</p>
        </div>
    </div>

    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-body p-4">
            <form asp-action="Edit">
                <input type="hidden" asp-for="IdDispendio" />

                <div class="mb-3">
                    <label asp-for="Descricao" class="form-label"></label>
                    <input asp-for="Descricao" class="form-control" />
                    <span asp-validation-for="Descricao" class="text-danger"></span>
                </div>

                <div class="form-check mb-3">
                    <input class="form-check-input" asp-for="ProjetoFinep" />
                    <label class="form-check-label" asp-for="ProjetoFinep"></label>
                </div>

                <div class="form-check mb-4">
                    <input class="form-check-input" asp-for="ProjetoLeiBem" />
                    <label class="form-check-label" asp-for="ProjetoLeiBem"></label>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Salvar Alterações</button>
                    <a asp-action="Index" class="btn btn-secondary ms-2"><i class="fas fa-arrow-left"></i> Voltar para Lista</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    .card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 30px rgba(0,0,0,0.2);
    }

    .card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
</style>

